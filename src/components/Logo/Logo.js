import React from 'react';
import burgerLogo from '../../assets/logo.png';

const logo = (props) => {
    return(
            <img src={burgerLogo} alt="logo" />
    );
}

export default logo;
