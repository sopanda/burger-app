import React from 'react';
import burgerLogo from '../../assets/logo_new.png';

const logo = (props) => {
    return(
            <img src={burgerLogo} alt="logo" />
    );
}

export default logo;
